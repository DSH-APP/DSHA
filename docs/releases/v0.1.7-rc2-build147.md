# DSHA 0.1.7-rc2 · build 147

本地适配记录。上游 DSH 锁定为 `0.1.7-rc.2`，提交 `477b4f4`，版本码 147，包名
仍为 `com.dsh.client`，Standard/Low 使用 E7E3 历史发布证书。

## 上游 rc2

官方发布页：[dsh-v0.1.7-rc.2](https://github.com/deepseek-ai/deepseek-harness/releases/tag/dsh-v0.1.7-rc.2)。

- 新增定时任务与运行记录、快捷键查看/搜索/自定义/恢复、进行中会话启用新工具。
- 自动审阅拒绝后的人工继续入口、插件源切换提示、插件详情和设置页信息修复。
- 修复插件安装或设置保存中断后的持续失败，以及部分长对话无法继续发送。
- Agent 预设会话头标签和模型编辑入口已由上游接管；rc1 的自定义 Agent 预设头部切换、模型导航补丁在 rc2 退役，避免覆盖上游只读会话语义。

## 运行时与数据保护

- 127 个顶层 DSH 官方包升级到 rc2；新增 rc2 所需 Cordis peer
  `@deepseek-ai/cordis-plugin-group@1.0.4`，干净 `npm ci --ignore-scripts` 可复现。
- Linux arm64 runtime archive SHA-256：`18ca9a502d3784cf907b2848a434c12139251bce97c4d7a24adeece75b975c3a`。
- 受管运行时身份：`43a519902e14158149b7cb7b3879f9f887bbc2522285a9d3ac1fd9ea57798e4f`。
- descriptor 的 `dataRead` 保留 `dsh-0.1.7-rc.1` 与当前 `dsh-0.1.7-rc.2`；`dataWrite` 使用 rc2。rc1 原始会话、settings、Profile 和 preset 候选继续走已有快照/逐节读回/原生审阅链。
- rc1 迁移脚本的目标版本、预设 bundle 依赖和内置插件 peer 已同步到 rc2；脚本仍保留 rc1 名称和原件隔离路径，保证旧记录可读。

## APK

| 包 | 大小 | SHA-256 |
|---|---:|---|
| `release/dsha-0.1.7-rc2.apk` | 269,209,896 | `50077f0e6d6c6487130c890bb82b6c2439d5b869cc0804dc5cc43be9463ee33f` |
| `release/dsha-0.1.7-rc2low.apk` | 345,498,795 | `fda9c39c6b647fcec3f373a8bb6edbe0a08d5a8bc84350ddd0ae4798dff24a60` |

两版均为 `com.dsh.client`、versionCode 147、arm64-v8a；Standard minSdk30，Low minSdk23，targetSdk37。E7E3 证书 SHA-256 为
`e7e3a31a75946f2669194c972b3dd0c9aea3fc7c50a8b885d2dee710b22a53f5`。

## 回归结果

- Standard/Low Release 编译、Release Lint、APK 资产和 DSH 版本门禁通过。
- Standard/Low Java 单测各 645 项：失败 0、错误 0、跳过 1；统一插件覆盖升级发布门禁通过。
- DeepSeek Messages、Lexical claim、conversation materialized、rc1 settings migration、profile settings host CLI、插件原生审阅、浏览器行为和 100 条消息渲染通过。
- V0/V1/V2/V3→V4、none/zstd、排他写句柄、旧 chronology 拒绝并保留原件：host 夹具 12/12；Linux arm64 夹具用于 APK/原生模块验证，不能在 Windows 进程中加载 Linux Koffi。
- 原有 rc1 插件页 Android 崩溃修复保留；rc2 上游插件详情/设置页变化未套用旧导航补丁。

## 真机范围

M2012K10C / Android 13 已从同签名 `0.1.7-alpha2`（versionCode145）直接执行 `adb install -r`
覆盖到 rc2（versionCode147）。`firstInstallTime=2026-09-21 15:06:35` 保持，数据目录
`/data/user/0/com.dsh.client` 保持；未卸载、清数据或格式化。首次维护约 120 秒完成，页面显示
“dsh 与内置插件更新完成；Ubuntu、个人目录、会话、配置和第三方插件保持原位”。随后 DSH
`0.1.7-rc.2` 在 3080 READY，已有会话内容可见；插件市场、插件管理、7 个插件列表和首项“更多”
操作均可打开，crash buffer 无 `com.dsh.client` 崩溃。设备未发送新的模型请求。

仍未宣称 Android 6/7、16 KiB 设备、所有厂商 ROM、所有外部模型服务或完整旧设备矩阵已覆盖。

随后在同一设备用同签名 `dsha-0.1.7-rc2low.apk` 覆盖安装，版本显示
`0.1.7-rc2low`，`firstInstallTime` 仍保持；Low 的 DSH/Web、Gecko 工作台、工作区菜单、
插件市场、插件管理和 7 个插件列表均可进入，crash buffer 无应用崩溃。最后再次用
`dsha-0.1.7-rc2.apk` 覆盖回 Standard，版本显示 `0.1.7-rc2`，DSH 0.1.7-rc.2 在 3080
READY，当前设备最终停留在 Standard。
