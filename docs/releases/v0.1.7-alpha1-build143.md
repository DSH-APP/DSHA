# DSHA 0.1.7-alpha1 · build 143

本地 E7E3 正式覆盖包，包名 `com.dsh.client`，版本码 143。内置 DSH 为 `0.1.7-alpha.1`，Ubuntu 基础环境保持 10。

- Standard：`release/dsha-0.1.7-alpha1.apk`，SHA-256 `52cfc874edd44fbee17e7e93ee0e10277be5f658728b1453940584ef9449120`。
- Low：`release/dsha-0.1.7-alpha1low.apk`，SHA-256 `f2954afa300efcff77dba156bc8f543b8306be1b84dd41d76f3e985d9725a1b3`。

本轮同步了 0.1.7 的依赖树、V3→V4 会话迁移、DeepSeek Messages 新序列化接口、Agent 预设新 Host 接口、Web 0.1.7 的 `./` 鉴权重定向、profile 补丁数组和 settings profile 备份边界。Standard/Low 单测、Release Lint、真实运行时启动、插件加载、备份/插件门禁和 APK 资产签名检查通过。

手机已用 E7E3 正式包从 0.1.6-alpha2.1/142 覆盖到 0.1.7-alpha1/143；包名、签名和 firstInstallTime 保持不变，受管运行时更新完成并显示 READY。修复恢复入口等待停止任务的生命周期竞态后，手机已进入 `WebPreviewActivity` 并显示现有对话页面；没有清数据、卸载或格式化。
