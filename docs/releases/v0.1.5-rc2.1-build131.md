# DSHA v0.1.5-rc2.1 / build 131

2026-09-14 本地稳定性修订。未上传 GitHub。同日按用户最新要求完成正式标准版覆盖安装和正常启动/网页检查，随后替换 release 中同版本文件；不同历史版本保留。包名 `com.dsh.client`，标准版 Android 11+、兼容版 Android 6+，arm64；dsh `0.1.5-rc.2` 和 Ubuntu base 10 不变。APK 非 debuggable，沿用原发布证书，兼容版验证 V1 签名。

本版修复设备桥/LAN 的资源与 HTTP 边界；绑定插件冻结依赖、审阅和加载失败恢复；区分凭据错误；补齐保留副本和旧树的只读检查、导出与预检恢复。覆盖不归档 Ubuntu、先认证再恢复、维护/进程屏障、受损原件保留与不自动备份继续适用。没有新增自动删除。

两版各 520 项 JVM 测试：519 通过、1 原有跳过；两版 Lint、资产/ELF/签名通过。现有 API 33 / 4 KiB 非调试审计安装完成记录中的实际回归；Android 6/7、API 30、Android 16 平板、16 KiB 真机、完整真实旧备份、冷安装及长期能耗仍有缺口。完整逐项说明见 [稳定性验收](../stability-acceptance.md)。

| 文件 | 字节 | SHA-256 |
|---|---:|---|
| `dsha-0.1.5-rc2.1.apk` | 192813150 | `b8627520334f685a00f4e9e62cf31b4fa5b79b0ac87f0d5b40456b77317c93d8` |
| `dsha-0.1.5-rc2.1low.apk` | 269307506 | `d25a4b9e4bf27c526c081d7a503fba0863aa283cb287144e4e24225c5d75832c` |

每份 APK 有同名 `.apk.sha256`；目录 `F:\DSHA_RESTART\release`。证书 SHA-256：

```text
e7e3a31a75946f2669194c972b3dd0c9aea3fc7c50a8b885d2dee710b22a53f5
```

机器清单：`release/dsha-0.1.5-rc2.1-manifest.json`。当前源码基线为 `dca04aed7c1a1468827a953bfd6295fc3ca44170` 加原有及本轮未提交修改；机器清单关联完整实际补丁、修改文件快照、依赖/资产摘要和全部命令与验证报告。源码包不包含离线大资产，资产在工作区按摘要关联。

## 正式覆盖验收与清理补记

用户已要求停止生成独立测试 APK。两份审计应用及 87 份测试/旧验收安装包副本已删除，相关测试源码与历史报告保留；旧入口默认禁用。发布包继续使用 E7E3…证书。手机原安装使用 A3F4…，经用户确认，用该原证书签名同一份非调试正式标准版覆盖；代码/资源字节比对一致，未增加测试功能。应用自身受管更新完成，显示已就绪并成功打开真实网页，原会话及未发送草稿仍可见；未发送消息、未运行故障注入。手机安装用的临时 APK 和 idsig 已删除。

本次发布 APK 字节未改变，只替换常规同版本文件名及对应校验文件，删除 `-build131` 并列 APK。旧交付清单移至 `app/build/test-apk-cleanup/previous-build131-manifest.json` 留存，因此此前历史报告中的 APK 路径可能已被替换或清理。当前文件及手机验收状态以新的常规文件名清单为准。兼容版沿用先前记录的验证，本次正式手机覆盖检查为标准版，不扩大为全设备结论。
