# DSHA 0.1.6-alpha1 · build 134

2026-09-17 本地交付。根据本轮 17 项反馈修改原生页面、模型配置和 Web 交互。标准版与兼容版均为非调试 Release，包名 `com.dsh.client`，两版各 552 项单测：551 通过、1 跳过，无失败；完整 Release Lint 无 Error/Fatal。未上传发布。

## 逐项修改

| 用户反馈 | 本次实现与验证 |
|---|---|
| 1 设置图标 | 重画细线齿轮矢量图标，日夜主题共用。 |
| 2 待审阅入口、下方提示和备用文件选择器 | 移除市场和管理页的全局待审阅入口、相关提示和“其他文件选择器”。安装预览与逐项审阅启用保护保留。 |
| 3 重复的环境备份重建 | 设置首页与数据页移除，统一留在安装与环境。 |
| 4 关于里的引导 | 关于卡片页与旧弹窗均移除重看首次引导；首次安装流程保留。 |
| 5 原生模型配置 | 首次配置、启动快捷入口、配置页共用原生表单。支持现有/自定义提供方、协议、地址、密钥、模型目录、容量和图片能力、附加字段；不打开 WebView。后台连接 DSH 的真实设置与凭据服务，以修订号和字段级操作保存，空密钥保持旧值。原生新增测试提供方后，真实 Web UI 显示相同配置；已清理测试提供方和测试密钥。 |
| 6 返回箭头 | 统一矢量图标与居中触摸区域，移除永久蓝底；短屏和大字体检查可用。 |
| 7 版本更新 | 版本信息、通道选择、候选说明、受管更新、回退和变更记录使用卡片布局。检查更新连接正常；未执行降级或实际回退。 |
| 8 省电区域 | 标题、说明与开关并排，压缩高度，保持原有省电逻辑和用户开关值。 |
| 9 设备通道与 ADB | 通道拆为独立卡片；配对地址、端口和验证码直接显示，手动连接为卡片内页。连接摘要限高，完整记录在详情内滚动。修复配对后沿用旧连接端口：自动读码同时记录系统主页面连接端口，缺失时通过原生 NSD 重新发现。真机自动配对返回 PAIR_OK、CONNECT_OK 和 uid=2000。 |
| 10 Root / Shizuku | 分别提供使用步骤与真实验证。原生 Root 验证返回 uid=0；Shizuku 返回 uid=2000。Shizuku 管理器需先启动服务，本次通过其提供的 ADB 启动入口验证。 |
| 11 插件管理的本地插件区 | 导入和导出只保留在市场。管理页保留搜索、排序、更新、详情与逐项操作。缺失来源/位置明确显示未知，不留空冒充已知。 |
| 12 DSH alpha.1 功能 | 保留完整锁定运行时与 19 个额外官方功能包，包括 Browser Use、Computer Use、Auto review、Team、SSH 和 Python PTC 组件。Android Computer Use 真实 MCP 6 工具通过读屏、点击、输入、清空、返回、滑动及 PNG 图像验证。外部驱动、MCP 和 SSH 仍需相应服务与配置，见下方边界。 |
| 13 无障碍设置 | 新增“两步设置”页：定位系统服务、返回检查真实连接、提供直接可用的对话示例与厂商路径提示。截屏改用应用专属目录，不再依赖“所有文件访问”。修复首次授权弹窗关闭时短暂取不到窗口的问题，只等待窗口读取，不重放动作。 |
| 14 自创 Agent 预设 | 移除菜单与切换函数两处“四个预设”白名单，使用 Host 完整列表。真机通过官方 API 创建自建预设，Gecko 顶栏实际显示第五个选项；验证后删除，只保留原有预设。 |
| 15 子代理跳转 | 锁定的 React 组件直接处理触摸点击，停用重复合成按键的旧补丁；保留原生子会话地址回调。真实组件在 393px 与 920px 通过展开、子会话地址、再次关闭和点外关闭测试。未发起付费子代理任务。 |
| 16 自检与诊断 | 首页和分类结果卡片重做；每项打开详情，启动历史独立查看。检查结果来自真实环境/命令/身份与状态，未把未配置能力标成通过。 |
| 17 性能体验 | 环境/维护显示状态移到后台读取；进入市场不再扫描全部已装插件；管理页按可见区域渲染。在同样 7 个插件、相同屏幕配置下，挂载视图从 174 降至 124，RenderNode 使用从 356.43 KiB 降至 245.49 KiB。普通切页短测未显示稳定的总体帧率提升，不宣称全局性能倍增。 |

## 真机证据

- Redmi M2012K10C / Android 13 / 4 KiB 页；同包名、A3 历史签名、非调试代码覆盖安装。
- 标准版系统 WebView 116 与兼容版 Gecko 143 均实际打开 DSH；原有 alpha15 对话保留，标准版原有草稿和待发送附件仍在。
- 原生模型写入、凭据状态读回、网页显示一致，测试记录按唯一名称清理。
- Root、Shizuku 和自动 ADB 配对/连接实际响应；Computer Use 的真实 PNG 为 176,341 字节，测试时 MANAGE_EXTERNAL_STORAGE 保持 deny/default。
- 浅色、深色、英文、1080×1920 短屏及 1.3 倍字体检查；最终恢复 1080×2400、1.0 倍字体、简体中文和浅色。
- 两版 E7 发布 APK 与 A3 手机安装包的全部非签名 ZIP 项一致：标准版 1153 项、兼容版 1254 项；最终手机安装文件 SHA-256 与标准版 A3 候选一致。
- 首次授权读屏与自动配对验证使用普通截屏，不用会暂时抑制无障碍服务的 uiautomator dump。普通页面布局检查才使用 UI 层级读取。
- [真机页面记录](../../ui-preview/native-ui-build134-review.html)。原确认稿 `dsha-0.1.5-rc2.1-ui-v2.html` 保留。

## 运行时与范围

DSH 为 `0.1.6-alpha.1`，Ubuntu 基础环境仍为 **10**。本次 Web 适配及 Android MCP 资产变化走受管组件更新与隔离试运行，个人数据保持原位；没有重建 Ubuntu、卸载应用、清除数据或运行故障注入。后续纯原生 UI 调整未再次更换运行环境。

运行时身份：`66eb13a05fffcacd0a63daf0d99c1c5892c423508b75887607500f4d524be253`。

未把当前设备检查等同于所有环境验证：本轮未做清空后的冷安装、付费模型/子代理任务、真实 SSH 服务器或外部 Browser Use / Cua Driver 后端联调；未在 Android 6/7、16 KiB 或 HarmonyOS 设备重测。未执行会覆盖真实个人数据的恢复与回退。功能包完整性、接口参数/路由测试与实际外部服务连通性分别记录。

## 交付校验

发布证书 SHA-256：`e7e3a31a75946f2669194c972b3dd0c9aea3fc7c50a8b885d2dee710b22a53f5`。

- 标准版：[`dsha-0.1.6-alpha1.apk`](../../release/dsha-0.1.6-alpha1.apk)，216,946,678 字节
  - SHA-256：`e116ac7872bd0efb01c7a9a688eb0468d03ea22929ad64a8ad6236503bde5334`
- 兼容版：[`dsha-0.1.6-alpha1low.apk`](../../release/dsha-0.1.6-alpha1low.apk)，293,382,107 字节
  - SHA-256：`40cb76c3b90804c3054284a4f50a17d1d221cb4f097aa9c8bdaa56a9976106f3`

详细工作证据保存在 `app/build/ui-rebuild134/`，普通页面截图在 `app/build/ui-rebuild/`。原始调试材料不随公开文档发布。

## 验收收尾

临时开启的 DSHA 无障碍服务与 Shizuku 服务已正常关闭，恢复测试前状态；用户自行接入的 Wi-Fi 保留。屏幕操作需要时可按新引导重新开启。验收专用提供方、密钥、预设、PNG、脚本和手机临时签名 APK 均已清理；通过应用正常下载的本次脱敏日志留在 Download/DSHA，便于查看。手机最后为标准版 build 134，DSH 已通过普通停止操作停下。
