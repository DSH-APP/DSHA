# DSHA 应用与网页优化验收清单

本轮目标：完成前两轮审查提出的全部优先级优化，并重点修复、验证备份与恢复。完成以真实行为和证据为准，不能只以构建成功代替功能验收。

## 备份与恢复

- [x] 全量、对话、设置、插件四种范围的文件名、清单和实际内容一致；部分包不使用全量前缀。
- [x] 打包公开目录中的实际数据、会话附件、插件源码与必要设置；清单记录真实版本，排除可重建缓存。
- [x] 备份导出通过 URI 校验大小和摘要，旧系统写入采用临时文件提交；失败不覆盖已有有效备份。
- [x] 恢复前完整校验与预览影响范围，损坏归档、范围不符或存储不足时不破坏当前数据。
- [x] 全量和部分恢复均保持正确的会话索引、设置、插件及公开数据绑定；失败可以恢复原状态。
- [x] 接通每启动 N 次自动备份，记录最近成功/失败及可恢复备份，提供清晰状态和恢复入口。
- [x] 独立数据完成备份→修改→恢复对照测试，并覆盖 URI 导出、软链接、损坏包、范围隔离和失败回滚。

备份阶段的实测证据和旧系统覆盖边界见 [备份验收记录](optimization-backup-verification.md)。自动备份的综合启动验收见 [运行恢复与省电记录](optimization-runtime-verification.md)。

## 更新、插件与运行状态

- [x] APK 下载可在关闭页面后继续，支持通知、断点续传、进程重建恢复与取消；最终安装前仍完整核验。
- [x] 插件兼容范围使用 npm 一致的 SemVer 语义，覆盖缩略范围与预发布版本。
- [x] 插件任务展示阶段/进度并可安全取消；检查更新优先获取元数据，未更新时不重复下载完整包。
- [x] 连续启动失败后停止循环重启，显示失败阶段并提供安全启动、回退、诊断入口。
- [x] 提供持续运行/省电模式，按状态管理锁，并验证后台恢复与任务不被错误中断。

## App 内网页

- [x] 会话日志等下载实际保存到用户选择位置，支持进度、失败反馈和取消；WebView/Gecko 行为一致。
- [x] 系统返回优先关闭网页当前弹层、抽屉或预览，再处理页面历史与退出。
- [x] 页面重建恢复当前导航、阅读位置和未发送附件；保留已有文字草稿机制。
- [x] 文件选择失败提供备用选择器和可理解的错误；文件授权、大小限制与清理行为正确。

双内核手机验证、真实 dsh 页面检查与覆盖边界见 [App 内网页验收记录](optimization-web-verification.md)。

## dsha.cc 官网

- [x] 安装输入改变后不能继续唤起旧插件；相同来源重新生成时保留名称、版本和 SHA-256。
- [x] 当前指南/FAQ/下载文案与实际功能一致，历史实测版本保留并清楚标识。
- [x] 电脑端提供携带完整安装参数的二维码；手机端保留正常唤起与下载回退。
- [x] 窄屏导航可发现、可操作，320/390px 无页面溢出，主操作可达。
- [x] 增加真实交互回归，覆盖修改输入、重复生成、校验参数、搜索、重置、二维码和无 JavaScript 回退。
- [x] 构建产物与线上页面/接口匹配，部署后验证既有下载与站点行为。

官网部署与交互验证见 [官网验收记录](optimization-site-verification.md)。

## 交付检查

- [x] 相关 Java/Python/网页测试、双版本构建与 Lint 通过；增加关键生命周期与故障场景测试。
- [x] 在可用设备完成备份与核心功能验收，明确区分实测设备和静态/模拟覆盖。
- [x] 不覆盖历史发布文件，不递增离线环境版本，不卸载或清空用户主应用；记录改动、测试和剩余限制。

最终双版本文件、摘要、验证结果和设备覆盖边界见 [rc1.4 完整验收记录](release-rc1.4-2026-09-07.md)。
