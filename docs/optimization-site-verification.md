# 官网优化验收

2026-09-07 完成安装入口、二维码、指南及窄屏导航修复，已部署到 https://dsha.cc/。

- 来源输入改变后立即清除旧唤起链接、二维码、分享字段和地址栏参数。重新生成相同原始来源时，保留名称、版本与 SHA-256；改成其他来源时不沿用旧包元数据。
- 二维码与复制链接都使用完整 HTTPS 安装地址，手机唤起和未安装回退保留同一组参数。过长链接不截断，改用完整复制回退；复制权限失败后自动选中可见字段。
- 320px 下主导航改为“菜单”，所有入口都可见、可点击，触摸高度为 44px。Escape 关闭菜单并回到按钮。无 JavaScript 时导航换行展示，下载入口及安装说明仍可读。
- 指南不再声称网站无法唤起 App，下载按钮跟随当前发布清单。条目实际核对过的历史版本继续显示。

## 验证

`npm run check` 共 23 项通过，包含 8 组 DOM 交互回归。覆盖同源重复生成、修改来源、非法/重复参数、Android 回退、内置入口、长链接、复制拒绝、搜索空结果、重置、历史筛选与导航。

二维码由 [qrcode-generator](https://github.com/kazuhikoarase/qrcode-generator) 生成，使用独立 [jsQR 解码器](https://github.com/cozmo/jsQR) 读取实际输出图形的像素，确认获得的完整地址及参数相同。库固定为 2.0.4，MIT 许可证随静态产物提供；测试依赖不发送到网站浏览器。

实际浏览器验证：重复生成保留全部参数；修改来源后旧按钮失效；320px 五个导航入口无横向裁切；安装主操作可达；390px 搜索从 1/8 到 0/8，重置恢复 8/8，页面无横向溢出。无 JavaScript 的结构回退另通过 DOM/静态检查。线上浏览器加载后，完整唤起参数、二维码、复制字段和摘要说明均正确显示。

## 部署

- 构建号：`912fabb528cfceae`。
- 归档 SHA-256：`6030b8e0c0ba61c6e67cb82227473bb5954bec67aab2b5be6ca7c9e4224644ba`。
- 服务器验证全部资源摘要后原子切换，旧构建 `96d0840218b2b666` 保留；未改变 HTTPS 配置。
- 公网页面、目录/更新接口、域名关联、APK 大小、Range/非法 Range 与 404 检查通过。线上关键页面和资源与本地构建摘要一致，历史 rc1.1 下载地址继续可用。
- 官网继续提供当前已发布 rc1.3 的两个 APK。本轮新应用包另作本地交付，未提前替换公开更新清单。
