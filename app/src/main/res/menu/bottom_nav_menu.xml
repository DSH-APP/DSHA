<?xml version="1.0" encoding="utf-8"?>
<!-- 顺序按**使用频率**排，不按功能分类：
     启动（每次都点）→ 插件（常逛）→ 设置（偶尔）→ 终端（排查时才用）。
     原顺序把终端放第二位，是按「技术栈层次」排的，但那不是用户的动线。 -->
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@+id/nav_launch"
        android:icon="@drawable/ic_launch"
        android:title="@string/ui_m0005" />
    <item
        android:id="@+id/nav_plugins"
        android:icon="@drawable/ic_plugins"
        android:title="@string/ui_m0016" />
    <item
        android:id="@+id/nav_settings"
        android:icon="@drawable/ic_settings"
        android:title="@string/ui_m0023" />
    <item
        android:id="@+id/nav_terminal"
        android:icon="@drawable/ic_terminal"
        android:title="@string/ui_m0021" />
</menu>
