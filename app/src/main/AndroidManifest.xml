<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 手机存储工作区的权限按版本分三档，缺一档就会在那一档系统上「目录在但读不了」：
         · API 30+（Android 11 起）：MANAGE_EXTERNAL_STORAGE，要用户去系统设置拉开关；
         · API 29（Android 10）：分区存储已经生效，靠 requestLegacyExternalStorage
           退回旧行为 + WRITE_EXTERNAL_STORAGE 运行时权限（所以 maxSdkVersion 必须到 29，
           写 28 的话 Android 10 就成了真空档 —— 新权限还没有、旧权限已被排除）；
         · API 26~28：WRITE_EXTERNAL_STORAGE 运行时权限就够，本来就能直写公共目录。 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- WifiLock 用：熄屏后 WiFi 会进省电模式，3081 局域网桥与联网安装都会断 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- 流式悬浮条（把 agent 正在生成的内容显示在屏幕顶部）。
         默认关闭、需要用户在系统设置里单独授予；不给也只是这一个功能不可用。
         真正的「状态栏歌词」没有公开接口（只有 Flyme/exTHmUI 认 ticker flag，
         其余机型要 Xposed），所以走自绘悬浮窗这条免 ROOT 的路。 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- 位置：让 agent 能回答「我在哪」「附近有什么」这类问题。
         运行时权限，且 DSHA 侧还有一道开关（cap_location，**默认关**）——
         位置是能定位到人的信息，不该因为装了 App 就默认开着。
         标 required=false：没有 GPS 的设备（部分平板）照样能装。 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-feature
        android:name="android.hardware.location"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.sensor.light"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.flash"
        android:required="false" />
    <!-- agent 通过 3090 桥查已装应用/启动应用（App 层能力，不需要 ADB 或 Shizuku）。
         Android 11+ 不声明它 getInstalledPackages 只能看到极少数应用。 -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- thedjchi/Shizuku 式开机自启：授权后可在开机时自动开启无线调试（无需手动操作） -->
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />

    <!-- 用户数据（rootfs、会话、凭据）有自己的显式备份/恢复流程，绝不能交给系统 Auto Backup：
         它会把私有目录整棵上传或迁移，既可能泄露密钥，也可能带走数百 MB rootfs。 -->
    <application
        android:name=".DshaApp"
        android:allowBackup="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        android:theme="@style/Theme.DeepseekHarness">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- 长按启动器图标的快捷方式。官方把「跳到 App 的某个页面」划给 Shortcuts，
                 高频开关（启停 dsh）才交给快捷设置磁贴。 -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <activity
            android:name=".WelcomeActivity"
            android:exported="false"
            android:theme="@style/Theme.DeepseekHarness" />

        <activity
            android:name=".ExtractActivity"
            android:exported="false"
            android:theme="@style/Theme.DeepseekHarness" />

        <activity
            android:name=".AdbPairActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:label="ADB 无线配对"
            android:theme="@style/Theme.AppCompat.Dialog" />

        <!-- 分享目标：从任何 App 把文本、链接、文件送进 agent 能读到的目录。
             exported 必须为 true（分享目标的性质如此）——所以只做「写文件」这一件事，
             不解析、不执行、不据此启动任何东西。文件名会过滤路径分隔符与控制字符。
             无界面：透明主题 + noHistory + 立刻 finish，分享是动作不是页面。 -->
        <activity
            android:name=".ShareInboxActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:label="DSHA · 交给 agent"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
        </activity>

        <!-- 快捷设置磁贴：下拉通知栏一键启停 dsh。
             label 有 18 字符预算，icon 必须是 24×24dp 纯白矢量。
             permission 那行是系统要求的，少了系统不认。 -->
        <service
            android:name=".DshTileService"
            android:exported="true"
            android:icon="@drawable/ic_tile_dsh"
            android:label="DSH"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <service
            android:name=".HarnessService"            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- 无障碍服务：只用来读系统「无线调试」弹窗里的配对码（配对码 2 分钟失效，
             手抄错一位就得重来）。可读范围由 accessibility_service_config.xml 里的
             packageNames 限定为 com.android.settings —— 系统层面就不会把其他应用的
             画面投给我们，用户在无障碍详情页也能看到这个限制。 -->
        <service
            android:name=".DshaAccessibilityService"
            android:exported="false"
            android:label="DSHA 配对助手"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- 关键修复：Shizuku UserService 必须注册，否则 bindUserService 永久失败。
             lint 的 Instantiatable 检查在这里是误报：Shizuku 的 UserService 不继承
             android.app.Service，而是实现 AIDL 接口（IShellService.Stub），由 Shizuku
             在 :shizuku 进程里自己实例化。加了 debuggable false 后 debug 构建也会跑
             release 级 lint，于是这条误报会直接中断打包，所以精准忽略它。 -->
        <service
            android:name=".ShellService"
            android:exported="false"
            android:process=":shizuku"
            android:permission="moe.shizuku.manager.permission.API_V23"
            tools:ignore="Instantiatable" />

        <!-- 设备桥服务（普通后台服务）：3090 桥 + ADB 预热 + 配对弹窗监听 -->
        <service
            android:name=".DeviceBridgeService"
            android:exported="false" />

        <receiver
            android:name=".ConfirmReceiver"
            android:exported="false" />

        <!-- ADB 保活的 Doze 兜底闹钟接收器 -->
        <receiver
            android:name=".AdbKeepAliveReceiver"
            android:exported="false" />

        <receiver
            android:name=".BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- 通知卡片就地输入配对码（RemoteInput） -->
        <receiver
            android:name=".AdbPairReceiver"
            android:exported="false" />

        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <!-- SAF 文件提供器：让系统「文件」以及 MT 这类文件管理器能直接浏览 App 私有目录下
             的 rootfs。android:permission 是 MANAGE_DOCUMENTS —— 那是 signature 级权限，
             只有系统 DocumentsUI 持有，所以第三方 App 只能经系统的选择器间接访问，
             不能直接绑我们的 provider。暴露范围也只有 files/linux/ubuntu/root 这一棵树。 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.share"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/share_paths" />
        </provider>

        <!-- 开发者直连：只接受 adb shell（uid 2000）与 system 的调用，代码里每个入口方法
             都自己核对一次 UID。刻意没有用 android:permission —— 没有哪个现成权限能表达
             「只允许 adb」（signature 级第三方也可能持有，normal/dangerous 级谁都能申请），
             所以防线在 DevBridgeProvider.guard() 里。开关默认关，见配置页。 -->
        <provider
            android:name=".DevBridgeProvider"
            android:authorities="${applicationId}.dev"
            android:exported="true"
            android:grantUriPermissions="false" />

        <provider
            android:name=".DshaDocumentsProvider"
            android:authorities="${applicationId}.documents"
            android:exported="true"
            android:grantUriPermissions="true"
            android:permission="android.permission.MANAGE_DOCUMENTS">
            <intent-filter>
                <action android:name="android.content.action.DOCUMENTS_PROVIDER" />
            </intent-filter>
        </provider>
    </application>

</manifest>
